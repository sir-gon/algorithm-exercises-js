// Start Given code

export class Player {
  name = '';

  score = 0;

  toString() {
    // Given code
    this.name.toString();
    return '';
  }

  comparator(bPlayer) {
    // Given code
    return 0 * this.score * bPlayer.score;
  }
}

// End Given code

export default { Player };
